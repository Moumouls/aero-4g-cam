name: Generate Video

on:
    workflow_dispatch:

jobs:
    generate-video:
        runs-on: macos-latest

        timeout-minutes: 30

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4
              with:
                  lfs: true

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: "24.9"

            - name: Enable Yarn
              run: corepack enable

            - name: Get Yarn cache directory
              id: yarn-cache-dir-path
              run: echo "dir=$(yarn config get cacheFolder)" >> $GITHUB_OUTPUT

            - name: Cache Yarn dependencies
              uses: actions/cache@v4
              with:
                  path: |
                      ${{ steps.yarn-cache-dir-path.outputs.dir }}
                      node_modules
                  key: ${{ runner.os }}-yarn-${{ hashFiles('yarn.lock') }}
                  restore-keys: |
                      ${{ runner.os }}-yarn-

            - name: Install dependencies
              run: yarn install --immutable

            - name: Extract XAPK to split APKs
              run: |
                  # Extract XAPK (it's a ZIP file)
                  if [ -f "UBox.xapk" ]; then
                    echo "Extracting UBox.xapk..."
                    mkdir -p split-apks obb
                    unzip -q UBox.xapk -d extracted_xapk
                    
                    # Move APK files to split-apks directory
                    find extracted_xapk -name "*.apk" -exec mv {} split-apks/ \;
                    
                    # Move OBB files if they exist
                    if find extracted_xapk -name "*.obb" -type f | grep -q .; then
                      echo "OBB files found, extracting..."
                      find extracted_xapk -name "*.obb" -exec mv {} obb/ \;
                    fi
                    
                    # Cleanup
                    rm -rf extracted_xapk
                    echo "✅ XAPK extraction complete"
                  else
                    echo "⚠️ UBox.xapk not found, assuming split-apks already exist"
                  fi
            - name: Create .env file from secrets
              run: |
                  cat > .env << EOF
                  # Cloudflare R2 Configuration
                  R2_ACCOUNT_ID=${{ secrets.R2_ACCOUNT_ID }}
                  R2_ACCESS_KEY_ID=${{ secrets.R2_ACCESS_KEY_ID }}
                  R2_SECRET_ACCESS_KEY=${{ secrets.R2_SECRET_ACCESS_KEY }}
                  R2_BUCKET_NAME=${{ secrets.R2_BUCKET_NAME }}
                  R2_ENDPOINT=${{ secrets.R2_ENDPOINT }}

                  # UBox Credentials
                  UBOX_EMAIL=${{ secrets.UBOX_EMAIL }}
                  UBOX_PASSWORD=${{ secrets.UBOX_PASSWORD }}

                  # CI Environment
                  CI=true
                  EOF

            - name: Setup Java
              uses: actions/setup-java@v4
              with:
                  distribution: "temurin"
                  java-version: "17"

            - name: Setup Android SDK
              uses: android-actions/setup-android@v3

            - name: Cache Android SDK
              uses: actions/cache@v4
              id: android-cache
              with:
                  path: |
                      ~/.android/sdk
                      ~/.android/avd
                  key: ${{ runner.os }}-android-sdk-api33-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
                  restore-keys: |
                      ${{ runner.os }}-android-sdk-api33-
                      ${{ runner.os }}-android-sdk-

            - name: Accept Android licenses
              if: steps.android-cache.outputs.cache-hit != 'true'
              run: yes | sdkmanager --licenses || true

            - name: Install Android SDK components
              if: steps.android-cache.outputs.cache-hit != 'true'
              run: |
                  echo "📦 Installing Android SDK components (this may take 5-10 minutes on first run)..."
                  echo "⏰ Started at: $(date)"

                  echo "Installing system image (largest download ~800MB)..."
                  sdkmanager --install "system-images;android-33;google_apis;arm64-v8a"

                  echo "Installing SDK platforms and tools..."
                  sdkmanager --install "platforms;android-33"
                  sdkmanager --install "platform-tools"
                  sdkmanager --install "emulator"
                  sdkmanager --install "build-tools;33.0.0"

                  echo "✅ SDK components installed at: $(date)"

            - name: Verify SDK components (from cache)
              if: steps.android-cache.outputs.cache-hit == 'true'
              run: |
                  echo "✅ Using cached Android SDK components"
                  sdkmanager --list_installed | head -20

            - name: Create AVD
              run: |
                  if [ -d ~/.android/avd/test_avd.avd ]; then
                      echo "✅ Using cached AVD"
                  else
                      echo "📱 Creating AVD..."
                      echo "no" | avdmanager create avd \
                          --force \
                          --name test_avd \
                          --package "system-images;android-33;google_apis;arm64-v8a" \
                          --device "pixel_5"
                      echo "✅ AVD created"
                  fi

            - name: Start Android Emulator
              run: |
                  echo "🚀 Starting Android Emulator..."
                  $ANDROID_HOME/emulator/emulator \
                      -avd test_avd \
                      -no-snapshot-save \
                      -no-window \
                      -gpu swiftshader_indirect \
                      -noaudio \
                      -no-boot-anim \
                      -camera-back none \
                      -camera-front none \
                      -memory 2048 \
                      -cores 2 \
                      > /tmp/emulator.log 2>&1 &

                  echo $! > /tmp/emulator.pid
                  echo "✅ Emulator started with PID $(cat /tmp/emulator.pid)"

            - name: Wait for Emulator to boot
              run: |
                  echo "⏳ Waiting for emulator to boot..."

                  # Show emulator log in background for debugging
                  tail -f /tmp/emulator.log &
                  TAIL_PID=$!

                  adb wait-for-device
                  echo "📱 Device detected by ADB"

                  # Wait for boot to complete
                  boot_completed=false
                  for i in {1..180}; do
                      if adb shell getprop sys.boot_completed 2>/dev/null | grep -q "1"; then
                          boot_completed=true
                          echo "✅ Emulator booted successfully in $((i * 2)) seconds"
                          break
                      fi
                      if [ $((i % 15)) -eq 0 ]; then
                          echo "⏳ Still waiting for boot... ($((i * 2))s elapsed)"
                      fi
                      sleep 2
                  done

                  # Stop tailing the log
                  kill $TAIL_PID 2>/dev/null || true

                  if [ "$boot_completed" = false ]; then
                      echo "❌ Emulator failed to boot within 6 minutes"
                      echo "📋 Last 50 lines of emulator log:"
                      tail -50 /tmp/emulator.log
                      exit 1
                  fi

                  # Disable animations
                  adb shell settings put global window_animation_scale 0
                  adb shell settings put global transition_animation_scale 0
                  adb shell settings put global animator_duration_scale 0

                  # Show device info
                  echo "📱 Device info:"
                  adb shell getprop ro.build.version.release
                  adb shell getprop ro.product.model
                  adb devices -l

            - name: Start Appium server
              run: |
                  echo "📱 Starting Appium server..."
                  yarn exec appium --allow-cors > /tmp/appium.log 2>&1 &
                  echo $! > /tmp/appium.pid

                  # Wait for Appium to be ready
                  echo "Waiting for Appium to start..."
                  for i in {1..30}; do
                      if curl -s http://localhost:4723/status > /dev/null 2>&1; then
                          echo "✅ Appium is ready"
                          break
                      fi
                      if [ $i -eq 30 ]; then
                          echo "❌ Appium failed to start"
                          cat /tmp/appium.log
                          exit 1
                      fi
                      sleep 1
                  done

            - name: Run Automation
              run: |
                  # Make script executable
                  chmod +x scripts/install-app.sh

                  # Install APKs and prepare device
                  ./scripts/install-app.sh

                  # Run the camera recorder automation
                  VERBOSE=1 node src/automation/camera-recorder.js

            - name: Cleanup
              if: always()
              run: |
                  # Kill Appium server
                  if [ -f /tmp/appium.pid ]; then
                      kill $(cat /tmp/appium.pid) 2>/dev/null || true
                  fi
                  pkill -f appium || true

                  # Kill emulator
                  if [ -f /tmp/emulator.pid ]; then
                      kill $(cat /tmp/emulator.pid) 2>/dev/null || true
                  fi
                  adb emu kill 2>/dev/null || true

                  # Show logs if they exist
                  if [ -f /tmp/appium.log ]; then
                      echo "📋 Appium logs:"
                      tail -100 /tmp/appium.log
                  fi

                  if [ -f /tmp/emulator.log ]; then
                      echo "📋 Emulator logs:"
                      tail -100 /tmp/emulator.log
                  fi
